const assert = require('assert');

describe("There are colleges in NY", function() {
    describe("and the backend API is available", function() {
        it("then it should all the backend API", function() {
            assert.strictEqual(true, true);
        });
    });
});