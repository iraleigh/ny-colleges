import React from 'react';
import { render } from '@testing-library/react';
import PageNavigation from './PageNavigation';

test('PageNavigation TODO', () => {
  const { getByText } = render(<PageNavigation />);
});
